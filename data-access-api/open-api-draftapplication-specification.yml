openapi: 3.0.0
info:
  title: Generated API from Excel Spec
  version: 1.0.0
paths:
  /api/v0/draft-applications/{id}:
    get:
      summary: Get a draft by id
      operationId: getDraftApplicationById
      tags:
        - draftapplication
      parameters:
        - name: id
          in: path
          required: true
          description: The id of the application to retrieve
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DraftApplication"
        '400':
          description: 'Bad request'
        '401':
          description: 'Unauthorized'
        '403':
          description: 'Forbidden'
        '404':
          description: 'Not found'
        '500':
          description: 'Internal server error'
    patch:
      tags:
        - draftapplication
      summary: Update an application
      operationId: updateDraftApplication
      parameters:
        - name: id
          in: path
          required: true
          description: The id of the application to updated
          schema:
            type: string
            format: uuid
      requestBody:
        description: update an existing item
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DraftApplicationUpdateRequest'
      responses:
        '204':
          description: No content
        '400':
          description: 'Bad request'
        '401':
          description: 'Unauthorized'
        '403':
          description: 'Forbidden'
        '404':
          description: 'Not found'
        '500':
          description: 'Internal server error'

  /api/v0/draft-applications:
    post:
      summary: Submit a draft application
      operationId: createDraftApplication
      tags:
        - draftapplication
      requestBody:
        description: create a draft application
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DraftApplicationCreateRequest'

      responses:
        '201':
          description: 'Created'
        '400':
          description: 'Bad request'
        '401':
          description: 'Unauthorized'
        '403':
          description: 'Forbidden'
        '500':
          description: 'Internal server error'

components:
  schemas:
    DraftApplication:
      type: object
      properties:
            id:
              type: string
              format: uuid
              description: Unique ID of the application
            client_id:
              type: string
              format: uuid
              description: Unique client identifier
            provider_id:
              type: string
              format: uuid
              description: Unique ID of the provider
            additional_data:
                type: object
                additionalProperties: true
                description: A JSON representation of the additional data sent to the resource
    DraftApplicationUpdateRequest:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Unique ID of the application
        client_id:
          type: string
          format: uuid
          description: Unique client identifier
        provider_id:
          type: string
          format: uuid
          description: Unique ID of the provider
        additional_data:
          type: object
          additionalProperties: true
          description: A JSON representation of the additional data sent to the resource
    DraftApplicationCreateRequest:
      type: object
      properties:
        provider_id:
          type: string
          format: uuid
        client_id:
          type: string
          format: uuid
        additional_data:
          type: object
          additionalProperties: true
          description: A JSON representation of the additional data sent to the resource